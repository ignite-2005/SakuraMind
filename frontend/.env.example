# URL of the FastAPI inference backend (Hugging Face Spaces).
# SET  -> hosted mode: /api/analyze proxies audio to this backend.
# UNSET-> local mode:  /api/analyze spawns backend/inference.py as a subprocess.
INFERENCE_API_URL=https://ignite2005-sakuramind.hf.space
